# Ejemplo de variables de entorno para `stack01`
# Copia este archivo a `.env` y aporta valores reales (no lo añadas a git).

# Postgres
POSTGRES_DB=appdb
POSTGRES_USER=appuser
POSTGRES_PASSWORD=apppass

# WildFly (management user)
# No uses estas contraseñas en producción; usa un gestor de secretos o .env en el host
WILDFLY_ADMIN_USER=admin
WILDFLY_ADMIN_PASS=changeme
